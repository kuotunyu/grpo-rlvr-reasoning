# Preserve the pinned upstream Qwen license byte-for-byte on every platform.
LICENSES/QWEN-RESEARCH-LICENSE.txt -text

# Manifest hashes cover repository bytes, so every text artifact must remain LF.
results/*.json text eol=lf
results/*.jsonl text eol=lf
results/*.md text eol=lf
